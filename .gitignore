# User config — contains secrets (github_token), never commit this
config.yaml

# Window state (position/size) — per-machine, not portable
state.json

# Claude Code
.claude/

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/

# Notification focus signal files
_focus.vbs
_focus.sh
_focus_signal

# Generated checkbox glyph (PIL-rendered at startup for QSS indicator image)
_check.png

# PyInstaller build artifacts
build/
dist/
*.spec

# Built binaries (produced by CI, distributed via GitHub Releases)
ActionsMonitor.exe
ActionsMonitor-linux

# wingetcreate local output — belongs in microsoft/winget-pkgs, not here
/manifests/
